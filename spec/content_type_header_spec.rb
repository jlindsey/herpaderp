require File.expand_path(File.dirname(__FILE__) + '/spec_helper')

describe EmailTest::Headers::ContentType do
  it "should extend the base Header class"
  it "should parse a Content-Type with a boundary"
  it "should parse a Content-Type with a charset"
end

